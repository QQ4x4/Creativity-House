{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "releaseCommand": "php artisan migrate --force && php artisan storage:link && php artisan db:seed --class=StudentPortalSeeder --force"
  }
}
