/**
 * i18n settings — single source of truth for supported locales.
 * Used by middleware.js, getDictionary.js, and layout/page components.
 */

export const defaultLocale = 'ar';
export const locales = ['en', 'ar'];
